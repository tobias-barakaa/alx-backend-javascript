const getResponseFromAPI = () => {
  return new Promise((resolve, reject) => ({
    if (false) {
      reject(false);
    } else {
      resolve(true);
    }
  }));
};
export default getResponseFromAPI;
