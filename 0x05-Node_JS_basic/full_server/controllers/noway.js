    // static getAllStudentsByMajorAndType(req, res) {
    //     const { major, type } = req.params;
    //     res.status(200).send(`List: ${major}, ${type}`);
    // }
    // static getAllStudentsByField(req, res) {
    //     const { field } = req.params;
    //     res.status(200).send(`List: ${field}`);
    // }
    // static getAllStudentsByFieldAndMajor(req, res) {
    //     const { field, major } = req.params;
    //     res.status(200).send(`List: ${field}, ${major}`);
    // }
    // static getAllStudentsByFieldAndMajorAndType(req, res) {
    //     const { field, major, type } = req.params;
    //     res.status(200).send(`List: ${field}, ${major}, ${type}`);
    // }
    // static getStudent(req, res) {
    //     const { id } = req.params;
    //     res.status(200).send(`Student: ${id}`);
    // }
    // static getStudentByMajor(req, res) {
    //     const { id, major } = req.params;
    //     res.status(200).send(`Student: ${id}, ${major}`);
    // }
    // static getStudentByMajorAndType(req, res) {
    //     const { id, major, type } = req.params;
    //     res.status(200).send(`Student: ${id}, ${major}, ${type}`);
    // }
    // static getStudentByField(req, res) {
    //     const { id, field } = req.params;
    //     res.status(200).send(`Student: ${id}, ${field}`);
    // }
    // static getStudentByFieldAndMajor(req, res) {
    //     const { id, field, major } = req.params;
    //     res.status(200).send(`Student: ${id}, ${field}, ${major}`);
    // }
    // static getStudentByFieldAndMajorAndType(req, res) {
    //     const { id, field, major, type } = req.params;
    //     res.status(200).send(`Student: ${id}, ${field}, ${major}, ${type}`);
    // }
    // static createStudent(req, res) {
    //     res.status(200).send('Student created\n');
    // }
